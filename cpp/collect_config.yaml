upper_body:
  left_device: "/dev/ttyUSB3"
  right_device: "/dev/ttyUSB2"
  baudrate: 1000000

inspire:
  # HAND_IDs assigned via set_inspire_id.py (run once per hand).
  # Factory default is 1; flash the right hand to 2.
  left_id: 1
  right_id: 2
