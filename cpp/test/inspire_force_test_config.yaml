inspire:
  source: "usb"   # "usb" = direct serial to hands; "g1" = via G1 DDS bridge (inspire_g1)
  network_interface: "enx00e04c6803fc"  # used when source == "g1"
  left:
    enabled: false
    id: 1
  right:
    enabled: true
    id: 2

# How often to refresh the force readout, in Hz.
print_rate_hz: 10
