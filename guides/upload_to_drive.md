# Upload Training Data to Google Drive

After running postprocess, the training data should be zipped and uploaded to
Google Drive using [rclone](https://rclone.org/).

Upload the zipped data to the `uminoid/training` folder.
